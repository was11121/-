MYAGENT_DATA_DIR=./data
DATABASE_URL=
MODEL_API_KEY=
BASE_URL=https://api.deepseek.com
CURRENT_MODEL=deepseek-v4-flash
HOST=127.0.0.1
PORT=8091
COGNITIVE_ENGINE_LIBRARY=
PERSONALITY_MODEL=Minej/bert-base-personality
PERSONALITY_DISABLE_BERT=

# --- 联网搜索配置 (Web Runtime) ---
# searxng 本地实例（Docker: docker run -d -p 8080:8080 searxng/searxng）
SEARXNG_URL=http://localhost:8080/search
# Tavily API Key（https://app.tavily.com 注册免费获取；留空则尝试读取 ~/.dsh/.credentials.yaml）
TAVILY_API_KEY=
# 新加坡 SSH 中转（可选，直连被墙/超时时的兜底通道）
WEB_RELAY_SSH_HOST=47.79.237.188
WEB_RELAY_SSH_USER=admin
WEB_RELAY_SSH_KEY=C:\Users\wby15\.ssh\aliyun_dsh
WEB_SEARCH_TIMEOUT=20
